{
  "extends": "./tsconfig.json",
  "exclude": ["ormconfig.js", "migrations", "node_modules", "test", "dist", "**/*spec.ts"]
}

// NOTES
// Adding ormconfig.js file and migrations folder to tsconfig.build exclude so that typescript does not compile these files.
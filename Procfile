web: npm run start:prod

# NOTES
# Creating this file to add heroku configurations
# Here we will add web config to start our web application
# We will provide the command to start the app in production and heroku will automatically run the build command for use to build the application.